name: GtkExercise
version: 0.0.0.0
build-type: Simple
cabal-version: >= 1.10

executable Main
    hs-source-dirs: Src
    main-is: Main.hs
    default-language: Haskell2010
    build-depends: base >= 4.7 && < 5, containers >= 0.6, transformers >= 0.5
    other-modules:
